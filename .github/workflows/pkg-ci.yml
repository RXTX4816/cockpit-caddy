name: Packaging

on:
  push:
    branches: [main]

jobs:
  pkg-ci:
    uses: RXTX4816/cockpit-plugin-base-react/.github/workflows/pkg-ci-plugin.yml@main
    with:
      plugin-name: cockpit-caddy
      spec-file: cockpit-caddy.spec
      aur-pkgname: cockpit-caddy
    secrets: inherit
